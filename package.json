{
  "name": "natours",
  "version": "1.0.0",
  "description": "Node.js learning",
  "main": "server.js",
  "scripts": {
    "start:dev": "cross-env NODE_ENV=development node --env-file=.env server.js",
    "start:prod": "cross-env NODE_ENV=production node --env-file=.env server.js",
    "start:dev:watch": "cross-env NODE_ENV=development node --env-file=.env --watch server.js",
    "start:prod:watch": "cross-env NODE_ENV=production node --env-file=.env --watch server.js"
  },
  "author": "Nirmalya Ganguly",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "express": "^5.2.1",
    "mongoose": "^9.7.3",
    "morgan": "^1.11.0",
    "slugify": "^1.6.9",
    "validator": "^13.15.35"
  },
  "devDependencies": {
    "cross-env": "^10.1.0"
  }
}
